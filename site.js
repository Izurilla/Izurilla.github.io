const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');if(menuBtn&&nav){menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');}));}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target);}})},{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();function formatJournalDate(value){const date=new Date(value+'T12:00:00');return new Intl.DateTimeFormat('es-ES',{day:'numeric',month:'long',year:'numeric'}).format(date);}function buildJournalCard(entry){const article=document.createElement('article');article.className='journal-entry reveal visible';const paragraphs=String(entry.text||'').split(/\n\s*\n/).map(p=>`<p>${p}</p>`).join('');article.innerHTML=`<div class="journal-entry-meta"><time datetime="${entry.date}">${formatJournalDate(entry.date)}</time><span>${entry.project||'Cuaderno de autora'}</span></div><h3>${entry.title}</h3>${paragraphs}<div class="journal-entry-status"><span>Estado</span><strong>${entry.status||'En proceso'}</strong></div>`;return article;}if(Array.isArray(window.CUADERNO_ENTRIES)){const sorted=[...window.CUADERNO_ENTRIES].sort((a,b)=>String(b.date).localeCompare(String(a.date)));const journalContainer=document.getElementById('journal-entries');if(journalContainer){sorted.forEach(entry=>journalContainer.appendChild(buildJournalCard(entry)));}const homeContainer=document.getElementById('home-journal-entries');if(homeContainer){sorted.slice(0,3).forEach(entry=>homeContainer.appendChild(buildJournalCard(entry)));}}